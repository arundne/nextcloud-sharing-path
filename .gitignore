/build/
.DS_Store

# Nothing from certificate/ is tracked except the request and the issued
# certificate — private keys must never end up in the repository.
/certificate/*
!/certificate/*.csr
!/certificate/*.crt
